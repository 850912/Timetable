# Timetable 3.5.3 Build20 Final — CI Hotfix 2026-09-23

## CI 失败原因

GitHub Actions 在执行：

```text
./gradlew --no-daemon :shared:test :mobile:testDebugUnitTest :wear:testDebugUnitTest
```

时，`:shared:test` 已经通过，但 `:mobile:compileDebugKotlin` 在两个新增 Service 处失败。

### 1. ChinaWearBleReceiverService.kt

原实现使用：

- `dagger.hilt.android.AndroidEntryPoint`
- `javax.inject.Inject`

但当前 `mobile` 模块的 `build.gradle.kts` 没有应用 Hilt Android plugin，也没有声明 Hilt runtime/compiler 依赖；因此 `dagger`、`inject`、`AndroidEntryPoint` 均无法解析。

**修复：**保留现有手机端架构，不为单个 Service 引入 Hilt。改用项目已经存在的 `TimetableDatabaseProvider.importService(this)`，延迟初始化 `ImportService`。

### 2. WearProfileOpenReceiverService.kt

原实现继承 `androidx.lifecycle.LifecycleService`，但 `mobile` 当前没有声明 lifecycle-service 依赖，因此 `LifecycleService` 无法解析；同时连带导致 `packageManager`、`startActivity`、`stopSelf`、`START_NOT_STICKY` 的类型推导错误。

**修复：**改为标准 `android.app.Service`，显式实现 `onBind()` 返回 `null`。该 Service 只需要普通 `Context` 能力，因此不需要 LifecycleService。

## 修改范围

仅修改：

```text
mobile/src/main/java/com/hufeng943/timetable/sync/ChinaWearBleReceiverService.kt
mobile/src/main/java/com/hufeng943/timetable/sync/WearProfileOpenReceiverService.kt
```

未修改协议、BLE 帧、Transport Resolver、Room、导出逻辑或 Wear 模块。

## 二次检查

已完成：

- 两个文件括号/大括号平衡检查
- 禁止 `dagger.hilt` / `javax.inject` / `LifecycleService` 引用检查
- Manifest 中两个 Service 声明检查
- Service 全工程引用检查
- 与上一个 Final 包进行 SHA-256 文件级比较
- 确认只有上述两个源码文件发生变化

本地完整 Gradle 测试再次执行时，容器无法解析 `services.gradle.org`，因此 Gradle Wrapper 无法下载 9.4.1；这是执行环境网络限制，不是新的源码错误。

## GitHub Actions 建议验证

继续使用原来的测试命令：

```bash
chmod +x ./gradlew
./gradlew --no-daemon :shared:test :mobile:testDebugUnitTest :wear:testDebugUnitTest
```

并继续执行发布构建：

```bash
./gradlew --no-daemon :mobile:assembleRelease :wear:assembleRelease
```
